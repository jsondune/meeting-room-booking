ตอนนี้มีข้อมูลครบแล้ว ให้ผมสรุปให้ครับ:

superadmin / Admin@123
admin / Admin@123
approver / User@123
user1-20 / User@123

---

## 🔐 Password Algorithm

### Algorithm ที่ใช้: **Argon2id**

```php
// common/models/User.php - Line 353-358

public function setPassword($password)
{
    // Use Argon2id for password hashing (OWASP recommended)
    $this->password_hash = Yii::$app->security->generatePasswordHash($password, PASSWORD_ARGON2ID);
    $this->password_changed_at = date('Y-m-d H:i:s');
}

public function validatePassword($password)
{
    return Yii::$app->security->validatePassword($password, $this->password_hash);
}
```

### ทำไมถึงใช้ Argon2id?

| คุณสมบัติ | รายละเอียด |
|----------|------------|
| **OWASP Recommended** | แนะนำโดย OWASP สำหรับการเก็บรหัสผ่าน |
| **Memory-hard** | ป้องกันการโจมตีด้วย GPU/ASIC |
| **Side-channel resistant** | ป้องกันการโจมตีแบบ timing attack |
| **PHP 7.2+** | รองรับใน PHP เวอร์ชัน 7.2 ขึ้นไป |

### Password Strength Requirements

```php
$minLength = 8;           // อย่างน้อย 8 ตัวอักษร
$requireUppercase = true; // ต้องมีตัวพิมพ์ใหญ่
$requireNumber = true;    // ต้องมีตัวเลข
$requireSpecial = true;   // ต้องมีอักขระพิเศษ
```

---

## 👥 Users ในระบบ (Demo Data)

### บัญชีผู้ดูแลระบบ

| Username | Email | Password | Role | ชื่อ |
|----------|-------|----------|------|------|
| `superadmin` | superadmin@bizco.co.th | `Admin@123` | Super Admin | ผู้ดูแล ระบบ |
| `admin` | admin@bizco.co.th | `Admin@123` | Admin | แอดมิน ทั่วไป |
| `approver` | approver@bizco.co.th | `User@123` | Approver | ผู้อนุมัติ การจอง |

### บัญชีผู้ใช้ทั่วไป (20 users)

| Username | Email | Password | Role |
|----------|-------|----------|------|
| `user1` | user1@bizco.co.th | `User@123` | User |
| `user2` | user2@bizco.co.th | `User@123` | User |
| `user3` | user3@bizco.co.th | `User@123` | User |
| ... | ... | ... | ... |
| `user20` | user20@bizco.co.th | `User@123` | User |

---

## 🔑 สรุป Credentials สำหรับทดสอบ

```
┌─────────────────────────────────────────────────────────┐
│  🔴 Super Admin                                         │
│  Username: superadmin                                   │
│  Password: Admin@123                                    │
│  สิทธิ์: เข้าถึงทุกฟังก์ชัน + ตั้งค่าระบบ                   │
├─────────────────────────────────────────────────────────┤
│  🟠 Admin                                               │
│  Username: admin                                        │
│  Password: Admin@123                                    │
│  สิทธิ์: จัดการห้อง, ผู้ใช้, รายงาน                        │
├─────────────────────────────────────────────────────────┤
│  🟡 Manager                                            │
│  Username: manager                                     │
│  Password: User@123                                     │
│  สิทธิ์: อนุมัติ/ปฏิเสธการจอง                             │
├─────────────────────────────────────────────────────────┤
│  🟢 User                                                │
│  Username: user1 - user20                               │
│  Password: User@123                                     │
│  สิทธิ์: จองห้อง, ดูการจองของตัวเอง                       │
└─────────────────────────────────────────────────────────┘
```

### วิธีสร้าง Demo Data

```bash
# Run seeder เพื่อสร้างข้อมูลทดสอบ
php yii seeder/all

# หรือสร้างเฉพาะ users
php yii seeder/users
```